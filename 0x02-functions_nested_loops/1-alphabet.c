#include "main.h"

/**
 * print_alphabet - prints the alphabet, in lowercase
 * Return: Always nothing
 */

void print_alphabet(void)
{
	int _putchar(char);

	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
		_putchar('\n');
}
