#include "main.h"

/**
 *rev_string - changes strings to out put in reverse
 *@s: Char pointer paramete/array
 *Return: void
 */
void rev_string(char *s)
{
	int i;
	int n;
	int p;
	
	s = char ch[i++];
	for (i = 0; (s[i] != '\0'); i++)
	{
		s = s[i];
		n = i;
	}
	for (p = n; p >= 0; p--)
	{
		s = s[n];

	}
}

