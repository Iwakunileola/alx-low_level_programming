#include "main.h"

/**
 * _strlen - Prints a string
 * @s: Char pointer parameter
 * Return: void
 */

int _strlen(char *s)
{
	int size;
	int i;
	char str[size] = {*s};

	for (i = 0; i < size; 1++)
	{
		if (i == (size - 1))
		{
			return (i);
		}
	}
	
}
