#include "main.h"

/**
 *_strlen - Prints a string
 *@s: Char pointer parameter
 *Return: void
 */

int _strlen(char *s)
{
	int i;
	int size;
	char str[size] = s

	for (i = 0; i < size; i++)
	{
		if (i == (size--))
		{
			return (i);
		}
	}

}

