#include "main.h"

/**
 * *_strcmp - Compares strings
 * @s1: Destination parameter
 * @s2: Source parameter
 * Return: int
 */

char *leet(char *a)
{
	    char *h;
	        char *g;
		    int *k;
		        int i;
			    
			    h = "aeotl";
			        g = "AEOTL";
				    k = "43071";
				        
				        for (i = 0; a[i] != '\0'; i++)
						    {
							            for (; g[i] != '\0' || h[i] != '\0';)
									            {
											                if (a[i] == (g[i] || h[i]))
														            a[i] = *(k + );
													        }
								        }
} return (a);

}
