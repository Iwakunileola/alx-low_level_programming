#include "main.h"

/**
 * _memset - fills s with b till n bytes
 * @s: Destination parameter
 * @b: source parameter
 * @n: numbers of bytes to copy
 * Return: s
 */

char *_memset(char *s, char b, unsigned int n)
{
	(void)b;
	(void)n;
	return (s);
}
