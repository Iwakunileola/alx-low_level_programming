#include "main.h"

/**
 * _strpbrk - finds the first occurence of the first char of accept in s
 * @s: first string
 * @accept: second character
 * Return: s
 */

char *_strpbrk(char *s, char *accept)
{
	(void)accept;
	return (s);
}

